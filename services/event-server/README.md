# LiveScape event server

Local FastAPI service that validates normalized LiveScape events and broadcasts
them to connected renderer clients over WebSocket. See the repository root
[README](../../README.md) for setup and the [protocol reference](../../docs/protocol.md).
